<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.vic.shiro_app.mapper.UserMapper">
    <resultMap id="SysUser" type="com.vic.shiro_app.model.SysUser">
        <id column="user_id" property="userId" jdbcType="INTEGER"/>
        <result column="user_name" property="userName" jdbcType="VARCHAR"/>
        <result column="full_name" property="fullName" jdbcType="VARCHAR"/>
        <result column="password" property="password" jdbcType="VARCHAR"/>
        <result column="salt" property="salt" jdbcType="VARCHAR"/>
    </resultMap>

    <select id="getAllUsers" resultMap="SysUser">
      SELECT * from  sys_user
    </select>
    <select id="getUserByUserName" parameterType="String" resultMap="SysUser">
      SELECT * from  sys_user where user_name = #{userName}
    </select>

    <select id="getUserByUserId" parameterType="int" resultMap="SysUser">
      SELECT * from  sys_user where user_name = #{userId}
    </select>
</mapper>